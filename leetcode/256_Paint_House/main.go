package main



func minCost(costs [][]int) int {
	minAmount := 0

  previousHouses := make([]int, 0)
  


	for i := 0; i < len(costs); i++ {

	}
}

func pickUnusedColor(house []int, color int) (cost, colour int) {
  house = append(hous
}

func findMin(array []int) (cost, colour int) {
  cost = array[0]

  for i := 0; i < len(array); i++ {
    if cost > array[i] {
      cost = array[i]
      colour = i
    }
  }

  return
}